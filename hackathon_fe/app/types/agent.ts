export type Agent = {
    id: string
    name: string
    provider: string
    model: string
    avatarUrl?: string
    created_at: string
    last_updated: string
    status: number
    prompt_id?: string
}